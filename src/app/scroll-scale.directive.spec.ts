import { ScrollScaleDirective } from './scroll-scale.directive';

describe('ScrollScaleDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollScaleDirective();
    expect(directive).toBeTruthy();
  });
});
